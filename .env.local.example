# Supabase Configuration (Read-Only Access)
# Get these from your Supabase project settings
# Only need the anon (public) key for read-only access

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# That's it! No other environment variables needed
# No auth secrets, no service keys, no DB passwords
# Just public read access to the question database

# For production deployment:
# - Update NEXT_PUBLIC_APP_URL to your production URL
# - These public keys are safe to expose in client-side code
